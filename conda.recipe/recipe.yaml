schema_version: 1

context:
  version: ${{ env.get("VERSION", "0.0.0") }}

package:
  name: lembas-planingfsi
  version: ${{ version }}

source:
  path: ..

build:
  number: 0
  noarch: python
  script:
    - SETUPTOOLS_SCM_PRETEND_VERSION=${{ version }} python -m pip install . -vv --no-deps --no-build-isolation

requirements:
  host:
    - python >=3.11
    - pip
    - hatchling
    - hatch-vcs
  run:
    - python >=3.11
    - lembas
    - planingfsi

tests:
  - python:
      imports:
        - lembas_planingfsi
      pip_check: false

about:
  homepage: https://github.com/mattkram/lembas-planingfsi
  license: MIT
  license_file: LICENSE
  summary: Lembas plugin for planingfsi hydrodynamic simulations
  description: |
    A lembas plugin that provides case handlers for running planingfsi
    hydrodynamic simulations, including planing flat plate analysis.
